export default interface IConfig {
  suites: number;
  cards: number;
  cardColours: string[];
  cardSuites: number[];
  timerInterval: number;
  defaultBackgroundColour: string;
  defaultCardBackgroundColour: string;
  defaultCardOutlineColour: string;
  defaultCardBackground: string;
}
